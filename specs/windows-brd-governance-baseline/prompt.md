# Implementation Prompt — Windows Application Baseline for specstest

> Use this prompt with any AI-assisted development tool to implement this feature.
> Generated by Astra Spec-Driven Development.

## Context

You are implementing the **Windows Application Baseline for specstest** feature.
Feature reference: **56807**.
Follow the specification exactly. Do not add features not described in the spec.

## Files to Reference

- **Project Context:** `specs/.devx/project-context.md`
- **Current State:** `specs/.devx/current-state.md`
- **Implementation Check:** `specs/.devx/implementation-check.md`
- **Feature Change Map:** `specs/.devx/change-maps/windows-application-baseline-for-specstest.md`
- **Golden Repo Guidelines:** `specs/.devx/guidance/golden-repo-guidelines.md` (REQUIRED when present)
- **Golden Repo Sources:** `specs/.devx/guidance/golden-repo-sources.md` (REQUIRED when present)
- **Golden UI Design System:** `specs/.devx/skills/ui-design/golden-ui-design-system.md` (REQUIRED when present)
- **Specification:** `specs/windows-application-baseline-for-specstest/specs.md`
- **Implementation Acceptance Checklist:** `specs/windows-application-baseline-for-specstest/requirements.md`


## Skill Files (Use In Your AI Prompt)

Use these skill files explicitly when implementing this feature:
- `specs/.devx/skills/implement/SKILL.md`
- `specs/.devx/skills/validate/SKILL.md`
- `specs/.devx/skills/next/SKILL.md`
- `specs/.devx/skills/autopilot/SKILL.md`
- `specs/.devx/skills/ui-design/SKILL.md`

## Work Items

- **[56810]** Windows Application Foundation
- **[56843]** As a Desktop Engineer, I want to document the Windows packaging approach so that the specstest application can be built and distributed consistently (3 pts)
  Acceptance:
1. When a Desktop Engineer saves the packaging section for the specstest baseline, the system stores exactly one Windows packaging approach record linked to the baseline identifier and project context.
2. The packaging record cannot be marked complete or submitted for approval unless packagingApproach, version, and approvalStatus are all populated with non-blank values.
3. A user without the Desktop Engineer or reviewer role receives an authorization error and no packaging data is created or modified when calling the packaging update endpoint.
4. Each successful packaging update creates a new versioned audit entry containing baselineId, previous and new packaging values, acting user, timestamp, and approval action where applicable.
5. The current approved packaging record is retrievable through the packaging read endpoint within 2 seconds for a standard specstest baseline load.
- **[56851]** As a Deployment Manager, I want to define the Windows deployment method so that the specstest application can be released through an approved enterprise path (3 pts)
  Acceptance:
1. The Windows deployment method is documented in the architecture baseline and linked to the specstest project
2. The system prevents build finalization when the deployment method baseline is missing or not approved
3. Deployment method approvals and updates are captured in immutable audit history for governance review
4. User can define the Windows deployment method so that the specstest application can be released through an approved enterprise path through the documented happy-path flow
5. System validates required inputs before completing "define the Windows deployment method so that the specstest application can be released through an approved enterprise path"
- **[56859]** As a Business Analyst, I want to maintain the BRD as the authoritative requirement baseline so that the Windows application scope remains clear and reviewable (3 pts)
  Acceptance:
1. When the BRD baseline is retrieved for the specstest project, the response includes document status, version, sourceBasis, project linkage, and any available architecture traceability references.
2. A BRD baseline update is rejected with a validation error when status, version, sourceBasis, or project linkage is missing from the save request.
3. The system prevents a save operation from creating a second active baseline version for the same project requirement set and returns a duplicate-version error response.
4. A successful BRD baseline update persists the revised metadata and records an audit entry containing the editor identity, timestamp, and changed fields.
5. Users without the authorized analyst or reviewer role cannot update the BRD baseline and receive an authorization failure response.
- **[56867]** As a Project Manager, I want to control scope additions and integration changes so that the Windows application is delivered without unapproved requirements (3 pts)
  Acceptance:
1. The system blocks approval of scope additions and integration changes when no source-backed requirement is linked
2. Approved changes update requirement traceability records and remain associated with the specstest project baseline
3. All scope control decisions are retained in auditable approval history for governance and release review
4. User can control scope additions and integration changes so that the Windows application is delivered without unapproved requirements through the documented happy-path flow
5. System validates required inputs before completing "control scope additions and integration changes so that the Windows application is delivered without unapproved requirements"
- **[56932]** As a Business Analyst, I want to link each BRD requirement to its source requirement identifier so that scope decisions remain traceable (3 pts)
  Acceptance:
1. When creating or updating a BRD requirement record, the system requires a source requirement identifier before the record can be submitted into the controlled baseline workflow
2. The system validates that the entered source requirement identifier matches an approved source-backed scope item before saving the BRD requirement as baseline-eligible
3. The BRD requirement detail view displays the linked source requirement identifier as part of the requirement record during review and approval activities
4. If a user enters an unknown, unapproved, or inactive source requirement identifier, the system blocks the save and shows a specific validation error for that identifier
5. The system prevents persistence of BRD requirement records in baseline-controlled status when no valid source requirement link exists, including through direct API requests
- **[56940]** As an Audit Manager, I want to view change history for BRD requirement records so that document lineage remains verifiable (3 pts)
  Acceptance:
1. When an existing BRD requirement record is changed, the system stores a new immutable history entry containing the requirement record identifier, actor identity, UTC timestamp, changed field names, previous values, new values, and approval state at the time of change.
2. An Audit Manager can open the change history for a specific BRD requirement record and view entries in reverse chronological order without any control to edit or delete historical entries.
3. If a user without the required reviewer or audit permission requests BRD requirement history, the system denies access and returns an authorization error without exposing change details.
4. History retrieval for a BRD requirement record includes metadata changes and content changes in the same timeline so document lineage can be reconstructed from creation through approval-state transitions.
5. The system retains audit entries after subsequent edits to the same BRD requirement record, and previously stored entries remain unchanged when later updates occur.
- **[56875]** As a Security Administrator, I want to enforce role-based access and audit logging for the Windows application baseline so that controlled project records remain protected and reviewable (3 pts)
  Acceptance:
1. Only users whose assigned roles grant the requested action can create, update, or approve requirement, architecture, or deployment baseline records; all other modification requests return an authorization error and do not persist changes.
2. When a protected baseline record is created, updated, or approved, the system writes an immutable audit event containing actor identity, actor role, UTC timestamp, object type, object identifier, action, outcome, and changed field details where applicable.
3. Denied modification attempts against controlled baseline records are recorded in the audit log with the attempted action, target object, actor identity, timestamp, and denial outcome.
4. GET /api/audit-logs supports filtering by actor, object type, and date range for Security Administrator users, and non-authorized users cannot retrieve audit log results.
5. Protected security and audit endpoints require authenticated access, use encrypted transport, and complete authorization checks within 500 milliseconds for standard requests in a production-like environment.
- **[56883]** As a Requirements Manager, I want to manage BRD version and status transitions so that document control remains compliant and traceable (3 pts)
  Acceptance:
1. System permits only configured BRD lifecycle transitions and blocks invalid status changes based on current state and assigned role
2. System marks one current approved BRD version for the project while preserving superseded versions in read-only history
3. System captures status transition comments, actor identity, timestamp, and prior-to-new state values for every lifecycle change
4. User can manage BRD version and status transitions so that document control remains compliant and traceable through the documented happy-path flow
5. System validates required inputs before completing "manage BRD version and status transitions so that document control remains compliant and traceable"
- **[56891]** As a Stakeholder Reviewer, I want to review requirement entries in a formal approval workflow so that the BRD can progress through controlled governance gates (3 pts)
  Acceptance:
1. The system displays the selected BRD version's requirement entries, document metadata, and current workflow status when an assigned Stakeholder Reviewer opens a review in progress.
2. The system accepts approve, reject, and change request actions only from users assigned to the current BRD review stage and returns an authorization error for unassigned or unauthorized users.
3. The system blocks submission of reject and change request actions when comments are blank and preserves the current workflow status until valid comments are provided.
4. When a valid review action is submitted, the system updates the reviewer decision, recalculates the BRD review status, and records reviewer identity, action, comments, timestamp, and resulting status in the audit trail.
5. After a review action is saved, the system queues a notification to the document owner and displays the recorded decision in the BRD review history for that version.
- **[56899]** As an Approval Manager, I want to finalize BRD approval outcomes so that only reviewed requirements become the active project baseline (3 pts)
  Acceptance:
1. System activates a BRD version as the project baseline only after all required reviews and mandatory metadata checks are complete
2. System prevents unauthorized approvers or incomplete review packages from finalizing BRD approval
3. System records final approval or rejection outcome with approver identity, timestamp, and baseline effect in the audit trail
4. User can finalize BRD approval outcomes so that only reviewed requirements become the active project baseline through the documented happy-path flow
5. System validates required inputs before completing "finalize BRD approval outcomes so that only reviewed requirements become the active project baseline"
- **[56948]** As a Project Manager, I want build readiness to depend on BRD and architecture approval status so that build finalization does not bypass governance gates (3 pts)
  Acceptance:
1. When a build finalization request is submitted for a project whose linked BRD record is missing, not complete, or not in Approved status, the system rejects the request and returns a blocking reason that identifies the BRD condition.
2. When a build finalization request is submitted for a project whose linked architecture baseline is missing, not complete, or not in Approved status, the system rejects the request and returns a blocking reason that identifies the architecture condition.
3. The build readiness view shows separate statuses for BRD approval and architecture approval, and each non-ready state includes the specific record state such as Missing, Incomplete, Pending Review, or Rejected.
4. Every build gate evaluation creates an immutable audit entry containing projectId, buildRequestId, evaluated BRD status, evaluated architecture status, gate outcome, timestamp, and the acting user identity.
5. If a user attempts to finalize a build while either governance record is not ready, the system does not persist the build as Finalized and instead keeps the build in its pre-finalization state.
- **[56907]** As a Release Manager, I want to enforce the architecture readiness gate before build finalization so that incomplete baseline decisions do not block deployment preparation (3 pts)
  Acceptance:
1. System blocks build finalization when the linked architecture baseline record is missing, has incomplete status, or lacks approval for the selected project.
2. System lists each failed readiness condition separately, including missing or unapproved runtime prerequisites, packaging approach, and deployment method data.
3. When all required architecture baseline conditions are satisfied, the readiness evaluation returns a pass result and the project can proceed to build finalization.
4. Each readiness evaluation stores an audit entry containing project identifier, evaluator identity, evaluation timestamp, decision result, and the evaluated condition details.
5. Only users authorized under Release Manager access rules can request or view build readiness evaluations; unauthorized requests are rejected.
- **[56956]** As a Security Architect, I want the BRD and architecture baseline repository to enforce secure access and audit reporting so that governance evidence is available for enterprise review (5 pts)
  Acceptance:
1. System enforces role-based access control for BRD and architecture baseline records and protects all data in transit and at rest using enterprise-approved encryption
2. System produces audit reports showing who viewed, created, changed, approved, or attempted to modify controlled records and when each action occurred
3. System captures API and application security events including failed authorization, invalid input attempts, and rate-limit breaches for compliance monitoring
4. User can I want the BRD and architecture baseline repository to enforce secure access and audit reporting so that governance evidence is available for enterprise review through the documented happy-path flow
5. System validates required inputs before completing "I want the BRD and architecture baseline repository to enforce secure access and audit reporting so that governance evidence is available for enterprise review"
- **[56998]** As a Governance Lead, I want build finalization blocked until the architecture baseline is approved so that the Windows application follows formal stage-gate controls (3 pts)
  Acceptance:
1. When a user attempts to mark build finalization complete, the system blocks the action if the architecture baseline approval status is not "Approved".
2. The build finalization gate evaluates presence of documented architecture scope, client runtime prerequisites, packaging approach, and deployment method before allowing completion.
3. If any required architecture baseline element is missing or in a non-approved state, the system returns a blocking message that names each missing or unapproved element.
4. Only authenticated users with build finalization permission can attempt the completion action, and unauthorized users receive an access-denied response without changing build status.
5. Each blocked or successful build finalization attempt is written to an audit log with build identifier, user, timestamp, evaluated baseline status, and outcome.
- **[57006]** As a Solution Reviewer, I want unapproved integration assumptions flagged during baseline review so that the Windows application scope stays aligned to approved source requirements (2 pts)
  Acceptance:
1. Baseline review identifies and flags any external integration content that is not supported by an approved source requirement
2. Unsupported integration assumptions are excluded from the approved Windows application architecture baseline
3. Approved baseline retains traceability evidence showing that integration scope was validated against formal requirement control
4. User can I want unapproved integration assumptions flagged during baseline review so that the Windows application scope stays aligned to approved source requirements through the documented happy-path flow
5. System validates required inputs before completing "I want unapproved integration assumptions flagged during baseline review so that the Windows application scope stays aligned to approved source requirements"
- **[56916]** As an Enterprise Architect, I want to block unsupported external integration assumptions so that the specstest Windows application remains within approved scope (3 pts)
  Acceptance:
1. System rejects external integration proposals that do not reference an approved source-backed requirement linked to the project BRD baseline
2. System allows an external integration proposal to progress only when the referenced requirement has an approved scope status
3. System records every blocked or approved integration compliance check with actor identity, timestamp, proposal identifier, and decision outcome
4. User can block unsupported external integration assumptions so that the specstest Windows application remains within approved scope through the documented happy-path flow
5. System validates required inputs before completing "block unsupported external integration assumptions so that the specstest Windows application remains within approved scope"
- **[56924]** As a Stakeholder Reviewer, I want to review BRD entries in a standardized format so that I can assess requirements for the specstest Windows application efficiently (2 pts)
  Acceptance:
1. When a Stakeholder Reviewer opens a BRD entry, the review view shows the requirement title or statement, detailed content, source information, workflow status, version number, and baseline indicator in the same field order for every entry
2. Entries marked Draft, In Review, and Approved are visually distinguished with explicit status labels, and no status relies on color alone to communicate meaning
3. If multiple versions exist for the same BRD entry, the review view marks exactly one approved baseline version as current and labels superseded versions as non-active
4. A reviewer can access the standardized review format in read-only mode without being presented with edit controls or update actions
5. If required BRD metadata for the standardized review format is missing, the system displays a clear incomplete-entry message identifying the missing field instead of rendering a partial unlabeled layout
- **[56965]** As a Project Manager, I want to track architecture baseline review status so that build planning reflects approved documentation gates (2 pts)
  Acceptance:
1. The architecture baseline record exposes only the controlled review states "Draft", "Under Review", "Approved", and "Superseded", and rejects any other status value on create or update.
2. When a reviewer changes the architecture baseline status, the system stores the reviewer identity, previous status, new status, and change timestamp in a persisted status history entry.
3. The status may transition from Draft to Under Review, Under Review to Approved, and Approved to Superseded; direct transition from Draft to Approved is blocked with a validation error.
4. Project reporting for build planning displays the current architecture baseline status and a build gate indicator that is "Ready" only when the latest status is Approved.
5. A user without architecture review or project reporting access receives an authorization error when attempting to change review status or retrieve the build gate report.
- **[56973]** As an Information Security Analyst, I want to review runtime prerequisites against enterprise desktop security standards so that the Windows application baseline aligns with controlled Windows environments (2 pts)
  Acceptance:
1. The runtime prerequisite record stores each prerequisite component with its component name, version, enterprise security review outcome, and whether elevated installation rights are required.
2. The system requires a security review outcome from the controlled values "Compliant", "Compliant with Exception", or "Non-Compliant" before the prerequisite review can be marked complete.
3. For any prerequisite marked as requiring elevated installation rights or restricted endpoint exception, the review record captures the corresponding flag and reviewer comment explaining the condition.
4. The security review outcome for the runtime prerequisite set is retrievable as evidence linked to the Windows application baseline approval record.
5. A user without security review permission cannot create or update runtime prerequisite review outcomes and receives an authorization error.
- **[56981]** As a Release Manager, I want to confirm prerequisite readiness before final build approval so that packaging and deployment are based on supported client conditions (2 pts)
  Acceptance:
1. The build approval workflow requires a recorded prerequisite readiness status before final build approval can be marked complete.
2. If any documented client runtime prerequisite is missing, unapproved, or marked pending, the system blocks final build approval and displays the blocking prerequisite entries.
3. The prerequisite readiness record stores evidence references with the architecture baseline, including prerequisite name, status, approver, and approval timestamp.
4. Only authenticated users with release approval permissions can update prerequisite readiness status or complete the final approval step.
5. The prerequisite readiness view shows the current baseline of client runtime prerequisites associated with the Windows application release being approved.
- **[56989]** As a Support Lead, I want packaging and deployment decisions documented together so that operational teams can support installation and rollout issues consistently (2 pts)
  Acceptance:
1. Architecture baseline presents packaging approach and deployment method in a single controlled section for operational reference
2. Documentation identifies support-relevant deployment considerations for installation, update, and recovery scenarios
3. Approved packaging and deployment information is accessible to release and support stakeholders before build finalization
4. User can I want packaging and deployment decisions documented together so that operational teams can support installation and rollout issues consistently through the documented happy-path flow
5. System validates required inputs before completing "I want packaging and deployment decisions documented together so that operational teams can support installation and rollout issues consistently"
- **[56827]** As a Solution Architect, I want to define the specstest desktop application as a Windows application so that the project is implemented on the correct client platform (3 pts)
  Acceptance:
1. When a Solution Architect saves the specstest architecture baseline, the record stores applicationType as "Windows" and includes the associated projectId.
2. The system rejects any attempt to submit the baseline for review if applicationType, projectId, or requirementId is missing.
3. The system validates that the selected requirementId exists in the results returned for the same project from the project requirements source.
4. Each create or update to the Windows application baseline writes an audit entry containing baselineId, userId, timestamp, and changed fields.
5. Retrieving or saving the specstest architecture baseline completes within 2 seconds for standard project data volumes.
- **[56835]** As a Release Manager, I want to record supported Windows client prerequisites so that the specstest application can be deployed to compliant desktop environments (3 pts)
  Acceptance:
1. The specstest architecture baseline stores supportedWindowsOs and at least one runtime dependency entry when prerequisites are saved successfully.
2. The system rejects a prerequisite update if supportedWindowsOs is empty, any runtime dependency entry is blank, or any entry contains only whitespace.
3. The system prevents duplicate prerequisite entries based on duplicate dependency name or duplicate dependency name and version combinations within the same baseline update.
4. Each successful prerequisite update creates a versioned history record and an audit log entry containing baselineId, userId, timestamp, and changed prerequisite values.
5. Retrieving or saving prerequisite data for the specstest baseline completes within 2 seconds for standard project usage.



## Implementation Instructions

1. Read the local handoff files under `specs/.devx/`: project context, current state, implementation check, and this feature's change map
2. When Golden Repo guidance exists (under `specs/.devx/guidance/` or the ui-design golden files), you MUST read it before planning and reconcile each convention — apply it, or mark it not-applicable-to-this-stack with a one-line rationale — and include a short "Golden Repo Reconciliation" note in your output (verified local patterns override golden guidance on conflict)
3. Inspect existing code first using the change map; verify candidate files, routes, components, services, tests, and shared modules in the actual workspace
4. Create an implementation plan that prefers modifying existing code over creating parallel implementations
5. Read `specs/windows-application-baseline-for-specstest/specs.md` thoroughly before writing any code
6. Read this feature's Open Questions (`specs/.devx/features.json` `openQuestions[]`, or the `## Open Questions` section in `specs/windows-application-baseline-for-specstest/specs.md`). Interactive run: surface the consolidated list, ask the human user for a decision before coding, apply their answer, and record the decision plus a one-line rationale in `specs/windows-application-baseline-for-specstest/assumptions.md`. Unattended run: choose a reasonable assumption and record it plus a rationale in `specs/windows-application-baseline-for-specstest/assumptions.md` — never assume silently. A feature held at `needs-clarification` (tracker `NEEDS_CLARIFICATION`) has an unresolved blocking question and must be resolved and re-enabled before implementing
7. Implement each item from `specs/windows-application-baseline-for-specstest/requirements.md` one at a time
8. Validate each checklist item as you implement it

## Constraints

- Do not deviate from the specification
- Do not add features or behaviors not in the spec
- Follow the project's established patterns and conventions
- Existing code patterns override generic guidance when they conflict
- Do not rely on Astra or live Golden Repo access during IDE implementation
- Each implementation acceptance item in requirements.md must be satisfied

## Validation

After implementation, verify:
- [ ] Local handoff files were read before planning
- [ ] When Golden Repo artifacts exist, they were read and a "Golden Repo Reconciliation" note was produced (each convention Applied or Not-applicable-to-this-stack with rationale)
- [ ] Existing code was inspected before editing
- [ ] Open Questions were reviewed; each was surfaced/asked (interactive) or recorded with a decision + rationale in `specs/windows-application-baseline-for-specstest/assumptions.md` (unattended), and no blocking Open Question was left unresolved and unrecorded (never assumed silently)
- [ ] All implementation acceptance items in `requirements.md` are satisfied
- [ ] All user scenarios from `specs.md` work correctly
- [ ] No extra features were added beyond the spec
- [ ] Code follows project conventions
